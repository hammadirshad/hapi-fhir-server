spring:
  datasource:
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

logging:
  file:
    name: fhir-hapi-server.log
server:
  tomcat:
    accesslog:
      directory: access-logs